<%= form_for(@application_medium) do |f| %>
  <% if @application_medium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@application_medium.errors.count, "error") %> prohibited this application_medium from being saved:</h2>

      <ul>
      <% @application_medium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
