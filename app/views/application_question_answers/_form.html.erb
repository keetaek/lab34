<%= form_for(@application_question_answer) do |f| %>
  <% if @application_question_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@application_question_answer.errors.count, "error") %> prohibited this application_question_answer from being saved:</h2>

      <ul>
      <% @application_question_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_area :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
